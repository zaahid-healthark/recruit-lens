# dependencies
node_modules/

# env & secrets — only the examples are committed
.env
.env.*
!.env.example
google-credentials.json

# build output
dist/
build/

# runtime data
server/uploads/
uploads/
*.log

# expo / react-native (Continuous Native Generation — android/ios are regenerated by `expo prebuild`)
.expo/
mobile/android/
mobile/ios/
*.jks
*.keystore

# prisma
server/prisma/*.db

# os
.DS_Store
Thumbs.db
